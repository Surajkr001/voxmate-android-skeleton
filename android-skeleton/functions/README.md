Mock functions server

Run locally for testing the Android app demo:

```bash
cd functions
npm install
node index.js
```

This will start a simple Express server on http://localhost:5000 with a `/v1/chat` endpoint that returns a canned response.
